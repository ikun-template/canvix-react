# 序列化与持久化

## 要求

### 序列化

- Document 整体序列化为 JSON 格式
- 支持 Base64 编码用于网络传输
- 大文档的解码操作不得阻塞主线程

### 持久化

- 支持手动保存和自动保存
- 保存过程需有状态反馈（保存中 / 已保存 / 保存失败）
- 保存期间不阻塞编辑操作

### 剪贴板

- 支持 Page 和 Widget 级别的复制/剪切/粘贴
- 粘贴时需为所有实例重新分配唯一 ID，防止冲突
- 剪贴板数据格式需包含类型标识，用于区分不同级别的粘贴内容
